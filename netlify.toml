[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Nitro writes the server to .netlify/functions-internal on this preset.
# DATABASE_URL (Neon pooled) and BETTER_AUTH_* must be set in the Netlify UI.
